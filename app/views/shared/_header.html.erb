<header>
    <nav class="navbar navbar-expand-lg navigation navbar-dark bg-success">

        <!-- ブランド名 -->
        <%= link_to root_path, class: "navbar-brand" do %>
            <%= image_tag "logo.png" %>
        <% end %>  

        <!-- ハンバーガーメニュー -->
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <!-- ナビゲーション -->
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class='navbar-nav ms-auto main-nav align-items-center'>
                <li class="nav-item active">
                    <%= link_to 'ゲキカラ、ドウ？について', '#', class: "nav-link" %>
                </li>
                <li class="nav-item active">
                    <%= link_to 'ログアウト', logout_path, class: "nav-link", data: { turbo_method: :delete } %>
                </li>
            </ul>
        </div>
    </nav>
</header>
